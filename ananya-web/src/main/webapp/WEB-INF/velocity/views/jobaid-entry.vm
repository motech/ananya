#vxmlHeader()
<script srcexpr="$callerData"/>
<script src="$entryJs"/>
<script>
    <![CDATA[
    var controller = new EntryController(callerData);
    ]]>
</script>

<form id="controller">
    <block>
        <goto expr="controller.decideFlowForJobAid()"/>
    </block>
</form>

<form id="unregistered">
    <block>
        #play("${bundle.audio.url}${bundle.jobaid.audio.url}${bundle.jobaid.welcome}")
        #play("${bundle.audio.url}${bundle.jobaid.audio.url}${bundle.jobaid.detail}")
        #play("${bundle.audio.url}${bundle.jobaid.audio.url}${bundle.jobaid.need.to.register}")
        <goto next="$registerFlow"/>
    </block>
</form>

<form id="registered">
    <block>
        <goto next="$nextFlow"/>
    </block>
</form>
#vxmlFooter()